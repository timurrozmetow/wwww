# React Native / Metro and Expo resolve modules better with a hoisted layout.
node-linker=hoisted
# Expo & friends declare a large web of peer deps; don't fail install on mismatches.
strict-peer-dependencies=false
auto-install-peers=true
# Keep lockfile authoritative in CI-like runs.
prefer-workspace-packages=true
